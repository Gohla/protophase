namespace Protophase.Service {
    /**
    Delegate used for sending and receiving published messages.
    
    @param  obj The object that is published.
    **/
    public delegate void PublishedDelegate(object obj);
}